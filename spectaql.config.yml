# ===========================================
# SpectaQL Configuration
# EasyKonnect API Documentation
# ===========================================

spectaql:
  # Output directory for generated documentation
  targetDir: ./public/docs

  # Logo configuration
  logoFile: ./docs/assets/logo.png
  faviconFile: ./docs/assets/favicon.ico

  # Display options
  displayAllServers: true

introspection:
  # Use introspection from running server
  url: http://localhost:3000/api/graphql

  # Metadata file for additional documentation
  metadataFile: ./docs/api-metadata.json

  # Query name depth limit
  queryNameDepth: 10

  # Remove deprecated fields
  removeDeprecated: false

info:
  title: EasyKonnect API Documentation
  description: |
    # Welcome to EasyKonnect API
    
    EasyKonnect is a professional service marketplace platform connecting service users 
    with verified service providers in Nigeria.
    
    ## Overview
    
    This API provides comprehensive endpoints for:
    
    ### 🔐 Authentication & Authorization
    - User registration with email verification (6-digit OTP)
    - JWT-based authentication with access/refresh tokens
    - Role-based access control (SERVICE_USER, SERVICE_PROVIDER, ADMIN, SUPER_ADMIN)
    - Password reset with secure OTP flow
    - Account lockout protection (5 failed attempts = 30-minute lockout)
    
    ### 👤 User Management
    - Profile management and updates
    - Profile photo uploads via Cloudinary
    - Become a service provider (role upgrade)
    
    ### 🏪 Service Provider Features
    - Provider verification workflow (submit documents → admin review)
    - Business profile management
    - Service creation and management
    - Booking management (accept/reject/complete)
    - Revenue tracking and payouts
    
    ### 📋 Services & Categories
    - Service listing with pagination and filtering
    - Category-based organization
    - Service search by location, category, price
    - Service status management (DRAFT, ACTIVE, INACTIVE)
    
    ### 📅 Bookings
    - Create bookings with scheduled date/time
    - Booking status workflow (PENDING → ACCEPTED → IN_PROGRESS → COMPLETED)
    - Booking cancellation with reasons
    - Provider/User booking history
    
    ### ⭐ Reviews & Ratings
    - Post-completion reviews (1-5 stars)
    - Provider response to reviews
    - Rating statistics and averages
    
    ### ❤️ Favourites
    - Save favourite services
    - Favourite management
    
    ### ⚖️ Disputes
    - Raise disputes on completed bookings
    - Evidence upload support
    - Admin resolution workflow
    
    ### 💬 Real-time Messaging
    - WebSocket-based messaging (Socket.io)
    - Conversation management
    - Message read receipts
    - Typing indicators
    
    ### 🔔 Push Notifications (OneSignal)
    - iOS and Android push notifications
    - Device registration (`registerPushToken`)
    - Push preference management
    - Automatic notifications for bookings, messages, reviews
    
    ### 📍 Geolocation & Maps (Google Maps Platform)
    - **Nearby Providers**: Find providers within radius using Haversine formula
    - **Distance Calculation**: Get distance from user to each provider
    - **Geocoding**: Convert addresses to coordinates (backend)
    - **Places Autocomplete**: Address suggestions (frontend integration)
    - Configurable search radius (default 25km, max 100km)
    
    ### � Payment System (Paystack)
    - **Payment Initialization**: Start Paystack checkout (`initializePayment`)
    - **Payment Verification**: Verify after callback (`verifyPayment`)
    - **Wallet Payments**: Pay using wallet balance (`payWithWallet`)
    - **Webhook Handling**: Secure webhook with signature verification
    - **10% Platform Commission**: Automatically calculated
    
    ### 💰 Wallet System
    - **User/Provider Wallets**: Track balances (`myWallet`)
    - **Transaction History**: View all transactions (`myWalletTransactions`)
    - **Admin Adjustments**: Credit/debit wallets (`adjustWalletBalance`)
    - **Distributed Locking**: Prevent race conditions
    
    ### 🏦 Bank Account Management
    - **List Banks**: Nigerian banks supported by Paystack (`banks`)
    - **Verify Account**: Confirm account before adding (`verifyBankAccount`)
    - **Add/Remove Accounts**: Manage provider banks
    - **Default Bank**: Set primary withdrawal account
    
    ### 💸 Withdrawals
    - **Request Withdrawal**: Provider payout to bank (`requestWithdrawal`)
    - **Cancel Withdrawal**: Cancel pending requests (`cancelWithdrawal`)
    - **Admin Processing**: Approve/reject withdrawals
    - **Daily Limits**: ₦5,000,000 per day per provider
    - **Withdrawal Fee**: ₦50 per withdrawal
    
    ### 📊 Payment Analytics
    - **Provider Reports**: Earnings breakdown (`myEarningsReport`)
    - **Admin Analytics**: Platform-wide statistics (`adminPaymentAnalytics`)
    - **Top Earners**: Highest earning providers (`topEarningProviders`)
    - **Refund Stats**: Refund tracking (`refundStats`)
    
    ### �👨‍💼 Admin Features
    - User management (suspend/activate/ban)
    - Provider verification approval
    - Service moderation
    - Dispute resolution
    - Platform analytics
    - Payment management
    - Withdrawal processing
    
    ## Authentication
    
    The API uses **JWT (JSON Web Tokens)** for authentication with a dual-token system:
    
    | Token | Expiry | Purpose |
    |-------|--------|---------|
    | Access Token | 7 days | API authentication |
    | Refresh Token | 30 days | Token renewal |
    
    ### Getting Started
    
    1. **Register** using the `register` mutation
    2. **Verify Email** with 6-digit OTP sent to your email
    3. **Use Tokens** - Include access token in Authorization header:
    
    ```
    Authorization: Bearer <your_access_token>
    ```
    
    ### Token Refresh
    
    Use `refreshToken` mutation when access token expires to get new tokens.
    
    ### Token Invalidation
    
    All tokens are automatically invalidated when:
    - User changes password (`changePassword`)
    - User resets password (`resetPassword`)
    - User must re-login on all devices after these operations
    
    ## Security Features
    
    - 🔒 Password hashing with bcrypt (12 rounds)
    - 🔒 OTP hashing with SHA-256 (timing-safe comparison)
    - 🔒 Rate limiting per user/IP
    - 🔒 Token invalidation on password change/reset
    - 🔒 XSS prevention - all inputs sanitized
    - 🔒 NoSQL injection protection
    - 🔒 IDOR protection - users access own resources only
    - 🔒 Query depth limiting (max 10 levels)
    - 🔒 Request body size limit (1MB)
    - 🔒 Webhook signature verification
    - 🔒 Distributed locking for wallet operations
    - 🔒 Account lockout after failed attempts
    
    ## Rate Limits
    
    | Operation | Limit | Window | Block Duration |
    |-----------|-------|--------|----------------|
    | Login | 5 attempts | 5 minutes | 15 minutes |
    | Registration | 5 attempts | 1 hour | 1 hour |
    | Password Reset | 3 attempts | 1 hour | 1 hour |
    | OTP Verification | 5 attempts | 5 minutes | 10 minutes |
    | OTP Resend | 3 attempts | 5 minutes | 10 minutes |
    | File Uploads | 20 uploads | 1 minute | - |
    | Messaging | 60 messages | 1 minute | - |
    | Bank Operations | 5 requests | 5 seconds | - |
    | General API | 100 requests | 15 minutes | - |
    
    ## Error Handling
    
    All errors include:
    - `message`: Human-readable description
    - `extensions.code`: Machine-readable error code
    
    ### Common Error Codes
    
    | Code | Description |
    |------|-------------|
    | `UNAUTHENTICATED` | Missing or invalid authentication token |
    | `UNAUTHORIZED` | Insufficient permissions |
    | `USER_NOT_FOUND` | User does not exist |
    | `USER_ALREADY_EXISTS` | Email already registered |
    | `INVALID_CREDENTIALS` | Wrong email or password |
    | `EMAIL_NOT_VERIFIED` | Verification required |
    | `ACCOUNT_LOCKED` | Too many failed attempts |
    | `OTP_EXPIRED` | Verification code expired |
    | `INVALID_OTP` | Incorrect verification code |
    | `RATE_LIMITED` | Too many requests |
    | `VALIDATION_ERROR` | Invalid input data |
    | `INSUFFICIENT_BALANCE` | Not enough wallet balance |
    | `WALLET_LOCKED` | Wallet is locked for operations |
    | `DAILY_LIMIT_EXCEEDED` | Exceeded daily withdrawal limit |
    | `PAYMENT_EXISTS` | Payment already exists for booking |
    
    ## WebSocket Events
    
    Connect to WebSocket at the same base URL for real-time features:
    
    | Event | Description |
    |-------|-------------|
    | `newMessage` | New message received |
    | `messageRead` | Message marked as read |
    | `typing` | User is typing |
    | `notification` | New notification |
    | `userOnline` | User came online |
    | `userOffline` | User went offline |
    
    ## Contact & Support
    
    For API support: api-support@easykonect.com
    
  version: 3.0.0
  contact:
    name: EasyKonnect API Support
    email: api-support@easykonect.com
    url: https://easykonect.com/support
  license:
    name: Proprietary
    url: https://easykonect.com/terms

servers:
  - url: http://localhost:3000/api/graphql
    description: Local Development Server
    production: false
  - url: https://backend-ehtm.onrender.com/api/graphql
    description: Production Server
    production: true

# Extensions for additional documentation features
extensions:
  # Generate examples from the schema
  generateExamples: true
